{
  "name": "lumanin-spec",
  "private": true,
  "description": "Pinned Raycast API type definitions — the compatibility spec. Dev-only, never shipped, never imported at runtime.",
  "dependencies": {
    "@raycast/api": "1.104.24",
    "@raycast/utils": "2.3.0"
  }
}
